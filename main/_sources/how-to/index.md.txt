# How-to guides

The authoring workflow: producing a model that satisfies the profile, exporting it, delivering it, and checking it before review.

Not yet written. This is the gap the repository exists to close — the rules are documented and the checks are mechanical, but the workflow that produces a conforming model has never been written down. Until it is, [the profile](../profile/index.md) states the target and `gltf-check` says whether a file hits it.

```{toctree}
:maxdepth: 2
```
